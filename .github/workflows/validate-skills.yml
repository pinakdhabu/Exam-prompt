name: Validate Skills

on:
  push:
    paths:
      - 'skills/**/*.md'
      - 'scripts/validate-skills.sh'
  pull_request:
    paths:
      - 'skills/**/*.md'
      - 'scripts/validate-skills.sh'
  workflow_dispatch:

jobs:
  validate:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
      fail-fast: false
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Run comprehensive validation (${{ matrix.os }})
        run: bash scripts/validate-skills.sh
        shell: bash

      - name: Cross-platform script syntax check
        run: |
          echo "=== Checking shell scripts for cross-platform issues ==="
          for script in scripts/*.sh; do
            if command -v bash &>/dev/null; then
              bash -n "$script" && echo "OK: $script" || echo "FAIL: $script has syntax errors"
            fi
          done

  validate-universities:
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - uses: actions/checkout@v4

      - name: Validate university registry
        run: |
          cd universities
          if command -v python3 &>/dev/null; then
            python3 -c "
import json
reg = json.load(open('university-registry.json'))
for u in reg.get('universities', []):
    d = u.get('directory', '')
    if d and not __import__('os').path.isdir(d):
        print(f'WARN: registry entry {u[\"id\"]} -> {d}/ not found on disk')
    else:
        print(f'OK: {u[\"id\"]} -> {d}/ exists')
"
          else
            echo "WARN: python3 not available, skipping deep validation"
          fi

  check-website:
    runs-on: ubuntu-latest
    continue-on-error: true
    timeout-minutes: 2

    steps:
      - uses: actions/checkout@v4

      - name: Check if QP website is reachable
        run: |
          if curl -sI --max-time 5 'https://sppuquestionpapers.com' &>/dev/null; then
            echo "OK: sppuquestionpapers.com is reachable"
          else
            echo "WARN: sppuquestionpapers.com is unreachable (this is OK — offline mode works)"
          fi
