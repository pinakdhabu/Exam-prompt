# Schemas

JSON Schema definitions for the Exam Prompt system.

| File | Purpose |
|------|---------|
| `university-config.json` | Schema for university config files |
| `skills-manifest.json` | Schema for `skills/skills.json` manifest |
| `session-profile.json` | Schema for user session profiles |
